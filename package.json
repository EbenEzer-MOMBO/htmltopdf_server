{
  "name": "htmltopdf-server",
  "version": "1.0.0",
  "private": true,
  "description": "Mini relais HTML → PDF (Playwright / Chromium) pour Eventime",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "postinstall": "node scripts/install-browsers.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "playwright": "^1.52.0"
  }
}
